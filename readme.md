This assignment was complete garbage, and I ended up giving up on it.
The requirements were absolute trash.

For example, it asked for a mathematically nonsensical function that computes the cross product of two-dimensional vectors with no arguments and returns a two-dimensional vector. That makes no sense.

What’s even worse is the networking part — it feels like endianness isn’t even being considered.

And building an event loop with the specified number of files is clearly insufficient.

Anyway, I got bored with it, but at least I learned more modern C++ instead of sticking with C++98.
